---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';

const allPosts = await getCollection('blog');
const posts = allPosts
	.filter(p => (p.data.lang ?? 'ru') === 'ru')
	.sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

const langMeta = {
	ru: { label: 'РУ', text: 'Русский', flag: '🇷🇺' },
	et: { label: 'ET', text: 'Eesti',   flag: '🇪🇪' },
	fi: { label: 'FI', text: 'Suomi',   flag: '🇫🇮' },
};
---

<!doctype html>
<html lang="ru">
<head>
	<BaseHead
		title="Блог — Sonin Hambaravi"
		description="Статьи о стоматологии от доктора Дмитрия Сонина: имплантация, протезирование, гигиена. Русский, эстонский, финский."
	/>
</head>
<body class="bg-white text-slate-800 antialiased">
<Header />
<main>

	<!-- ═══ HERO ════════════════════════════════════════════ -->
	<section class="bg-gradient-to-br from-slate-900 via-blue-950 to-slate-900 text-white py-16 px-4 sm:px-6">
		<div class="max-w-6xl mx-auto text-center">
			<span class="inline-flex items-center gap-2 bg-blue-900/60 border border-blue-700/50 text-blue-300 text-xs font-semibold uppercase tracking-widest px-4 py-1.5 rounded-full mb-6">
				<span class="w-1.5 h-1.5 bg-blue-400 rounded-full"></span>
				Полезные материалы
			</span>
			<h1 class="text-4xl sm:text-5xl font-bold mb-4">Блог клиники</h1>
			<p class="text-slate-300 text-lg max-w-xl mx-auto">
				Советы по уходу за зубами, информация об имплантации и протезировании от доктора Дмитрия Сонина.
			</p>
		</div>
	</section>

	<!-- ═══ ФИЛЬТРЫ + СТАТЬИ ═════════════════════════════════ -->
	<section class="py-16 px-4 sm:px-6">
		<div class="max-w-6xl mx-auto">

			<!-- Сетка статей -->
			<ul id="posts-list" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 list-none p-0 m-0">
				{posts.map((post) => {
					const meta = langMeta[post.data.lang ?? 'ru'];
					return (
						<li data-lang={post.data.lang ?? 'ru'} class="flex flex-col">
							<a href={`/blog/${post.id.replace(/\.(mdx?)$/, '')}/`}
								class="group flex flex-col h-full bg-white rounded-2xl border border-slate-100 shadow-sm hover:shadow-md hover:-translate-y-1 transition-all duration-200 overflow-hidden no-underline">

								<!-- Превью -->
								{post.data.heroImage
									? <img
											src={post.data.heroImage}
											alt={post.data.title}
											width={720}
											height={360}
											class="w-full h-44 object-cover"
											loading="lazy"
										/>
									: <div class="w-full h-44 bg-gradient-to-br from-blue-50 to-slate-100 flex items-center justify-center">
											<svg class="w-10 h-10 text-blue-200" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.2">
												<path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
											</svg>
										</div>
								}

								<div class="p-6 flex flex-col flex-1">
									<!-- Дата -->
									<div class="flex items-center gap-2 mb-3">
										<span class="text-slate-400 text-xs">
											<FormattedDate date={post.data.pubDate} />
										</span>
									</div>

									<!-- Заголовок -->
									<h2 class="text-base font-bold text-slate-900 group-hover:text-blue-600 transition-colors leading-snug mb-2 flex-1">
										{post.data.title}
									</h2>

									<!-- Описание -->
									{post.data.description && (
										<p class="text-slate-500 text-sm leading-relaxed line-clamp-2 mb-4">
											{post.data.description}
										</p>
									)}

									<!-- Автор + ссылка -->
									<div class="flex items-center justify-between mt-auto pt-4 border-t border-slate-100">
										<span class="text-xs text-slate-400">{post.data.author}</span>
										<span class="text-blue-600 text-xs font-semibold group-hover:gap-2 flex items-center gap-1 transition-all">
											Читать
											<svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
												<path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
											</svg>
										</span>
									</div>
								</div>
							</a>
						</li>
					);
				})}
			</ul>

		</div>
	</section>

</main>
<Footer />

</body>
</html>
